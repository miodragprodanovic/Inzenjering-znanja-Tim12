import { GetBetterComponent } from './get-better-component';

describe('GetBetterComponent', () => {
  it('should create an instance', () => {
    expect(new GetBetterComponent()).toBeTruthy();
  });
});
